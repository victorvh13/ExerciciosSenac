git init

git remote add origin https://github.com/victorvh13/ExerciciosSenac.git

git add.

git commit -m "primeiro commit"

git push --set-upstream origin master

git clone + endereço do repositorio para clonar arquivos para o repositório local